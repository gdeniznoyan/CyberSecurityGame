import{exportArchitectureState,importArchitectureState}from"./architectureState.js";import{initializeDragAndDrop}from"./dragdrop.js";import{initializeImageManager}from"./imageManager.js";import{analyzeArchitecture,renderArchitectureResult}from"./resultPlaceholder.js";import{configureStateDialog,renderAll,showError}from"./renderer.js";import{resetState,subscribe}from"./state.js";
const byId=<T extends HTMLElement>(id:string):T|null=>document.getElementById(id)as T|null;
function initialize():void{renderAll();subscribe(renderAll);initializeImageManager(renderAll);initializeDragAndDrop(showError,renderAll);configureStateDialog();renderArchitectureResult(null);
byId<HTMLButtonElement>("reset-button")?.addEventListener("click",()=>{resetState();showError("");renderArchitectureResult(null);const textarea=byId<HTMLTextAreaElement>("state-json");if(textarea)textarea.value=""});byId<HTMLButtonElement>("analyze-button")?.addEventListener("click",()=>{const result=analyzeArchitecture();renderArchitectureResult(result)});
const dialog=byId<HTMLDialogElement>("state-dialog"),textarea=byId<HTMLTextAreaElement>("state-json"),help=byId<HTMLElement>("dialog-help"),copy=byId<HTMLButtonElement>("copy-json-button"),apply=byId<HTMLButtonElement>("apply-import-button"),error=byId<HTMLElement>("dialog-error");byId<HTMLButtonElement>("export-button")?.addEventListener("click",()=>{if(!dialog||!textarea||!help||!copy||!apply)return;textarea.value=exportArchitectureState();help.textContent="Copy the current architecture JSON.";copy.hidden=false;apply.hidden=true;dialog.showModal()});byId<HTMLButtonElement>("import-button")?.addEventListener("click",()=>{if(!dialog||!textarea||!help||!copy||!apply)return;textarea.value="";help.textContent="Paste architecture-state JSON, then choose Import JSON.";copy.hidden=true;apply.hidden=false;dialog.showModal();textarea.focus()});copy?.addEventListener("click",async()=>{if(!textarea)return;try{await navigator.clipboard.writeText(textarea.value);if(help)help.textContent="JSON copied."}catch{textarea.select();document.execCommand("copy")}});apply?.addEventListener("click",()=>{if(!textarea||!error||!dialog)return;const result=importArchitectureState(textarea.value);error.textContent=result.errors.join(" ");if(result.valid){dialog.close();showError("")}})}
initialize();
